# VTM Feature Matrix (Viettinmart vs WKComputer Multisite)

| Feature | Reference Behavior (Viettinmart) | Current Project (WKComputer) | Gap Identified | Implementation Delivered | Test Verification | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Multisite Core & Tenant Isolation** | Runs on `viettinmart-eco` (`project_id = 10`, `tenant_id = 3`) with database scoping. | Runs on `wkcomputer` (`project_id = 14`, `tenant_id = 4`) on shared core. | Cross-tenant middleware previously did not verify non-superadmin project IDs. | Updated `CheckCmsRole.php` to enforce `$project->id` check against `$user->project_ids`. Added `BelongsToTenant` & `ProjectScoped` across all models. | `scratch/test_security_penetration.php` (SEC-001/002 return 403), `MultisiteTenantIsolationTest.php` | **PASS** |
| **Admin CMS Authentication** | Authenticated via `/{projectCode}/login` (`ProjectLoginController`) and separate session guard. | Customer login was clashing at `/login` and `/register`. | Customer routes collided with CMS admin login routes. | Namespaced customer auth to `/khach-hang/dang-nhap` and `/khach-hang/dang-ky`. Dedicated CMS admin at `/wkcomputer/login`. | `scratch/test_cms_login.php`, `scratch/test_frontend_routes.php` | **PASS** |
| **Product & Catalog Management** | Products stored in `posts` (type `product`) & `products_enhanced`, categories in `taxonomies` & `product_categories`. | WK had 1,035 products and 32 categories in `products_enhanced` but missing in `posts`/`taxonomies`. | CMS Admin lists products from `posts` table, so WK admin showed 0 products initially. | Created bidirectional catalog synchronization script (`scratch/sync_wk_catalog.php`) and integrated step 6 into `WkcomputerProductsSeeder.php`. | `/wkcomputer/admin/products` renders 1,035 products; `/wkcomputer/admin/categories` renders 32 categories. CRUD verified. | **PASS** |
| **Category Hierarchy & CRUD** | Nested categories with parent-child relationships, slug generation, SEO metadata. | Categories were only flat in `product_categories`. | Admin category CRUD needs synchronization between `taxonomies` and `product_categories`. | Verified two-way category CRUD via Admin `CategoryController`. Isolated by `project_id = 14`. | `scratch/test_admin_crud.php` (Created, Verified, Zero leaks to VTM) | **PASS** |
| **Widget Engine & Placements** | Dynamic widget rendering on Homepage, Header, Footer via `WidgetRegistry` and `app/Widgets/`. | WKComputer widgets existed in `widgets` table but lacked `tenant_id` assignment. | Widgets missing `tenant_id` and fallback metadata when JSON definitions missing. | Added `BelongsToTenant` & `ProjectScoped` to `App\Models\Widget`. Added metadata fallback in `WidgetRegistry.php`. | `scratch/test_widget_crud.php` (Create, Toggle, Update, Delete all HTTP 200/302 PASS) | **PASS** |
| **Settings & 21 Core CMS Modules** | 21 Settings modules (General, Contact, Social, SEO, Header, Footer, Email, Payment, Shipping, etc.). | Settings stored in `settings` and `project_settings` table. | PHP fatal error in `social.blade.php` (`getSocialValue` redeclaration) and connection switching bug during testing. | Wrapped helper in `if (!function_exists(...))`. Fixed connection switching in `SettingsController.php`. Verified all 21 modules. | `scratch/test_settings_parity.php` (21/21 PASS on both VTM and WK), `AdminCmsParityTest.php` | **PASS** |
| **Form System & Contact Submissions** | Custom form submission storage with anti-XSS sanitization and status workflow. | WK had contact form without backend persistence to `form_submissions` table. | Contact submissions were not stored in DB or visible in Admin CMS. | Added `BelongsToTenant` & `ProjectScoped` to `FormSubmission`. Updated `Wkcomputer\ContactController::send` to persist to DB. Created `cms.form-submissions.show` view. | `scratch/test_form_submissions_http.php` (Submit -> Admin List -> Detail -> Status Update -> Delete -> Cross-Tenant 404 PASS) | **PASS** |
| **PC Builder Tool** | High-performance interactive configurator for custom PC component assembly. | Full PC builder UI and JSON endpoints present at `/wkcomputer/xay-dung-cau-hinh`. | URL aliases and component category filtering needed validation. | Implemented dedicated `BuildPcController` and API endpoints with full product catalog. | Browser subagent validation + HTTP 200 verification. | **PASS** |
| **Cart & Checkout Workflow** | Session-based cart with coupon calculation, order creation, and confirmation. | Full cart & checkout engine present in `Wkcomputer\CartController` & `CheckoutController`. | `/thanh-toan` returned 404 because route was named `/dat-hang`. | Added route aliases for `/thanh-toan` and `/tin-tuc` in `routes/wkcomputer.php`. | Verified `/wkcomputer/gio-hang` and `/wkcomputer/thanh-toan` HTTP 200. | **PASS** |
| **Frontend Vanilla HTML Preservation** | Pure Blade templates styled with CSS in `public/theme/` and assets in `public/themes/`. | WKComputer templates preserved in `resources/views/pages/` and assets in `public/themes/wkcomputerdemo/`. | No frontend framework conversion permitted (no React/Vue/Next.js). | Preserved 100% vanilla HTML/Blade structure and static asset directory mapping. | Browser subagent visual inspection and route audits. | **PASS** |
| **Security & Penetration Checks** | Role-based gate checks, strict CSRF token validation, XSS escaping in Blade. | Needs audit against cross-tenant hijacking, SQL injection, and parameter tampering. | Default role middleware allowed project enumeration if user logged in. | Enforced strict project list verification in `CheckCmsRole.php`. Parameterized Eloquent queries. `strip_tags` sanitization in forms. | `scratch/test_security_penetration.php` (6/6 PASS). Zero cross-tenant leaks. | **PASS** |
| **SuperAdmin 1-Click Deploy** | SuperAdmin dashboard allows provisioning new tenants and running seeders. | Deploy service in `WkcomputerDeployService.php`. | Needed verified seeder idempotency so re-deploy does not duplicate data. | Seeders use `updateOrInsert` and `firstOrCreate` with project/tenant scoping. | `WkcomputerDeployService::deploy()` tested with full catalog. | **PASS** |
